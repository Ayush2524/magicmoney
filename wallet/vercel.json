{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/((?!ledger-api).*)",
            "destination": "/index.html"
        }
    ]
}
